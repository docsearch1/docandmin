import 'package:flutter/material.dart';

class Refers extends StatelessWidget {
  static const String routeName = "/Reffers";

  const Refers({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Scaffold(
        appBar: AppBar(
          title: const Text(
            "This Is Reffers Page is uploaded soon",
          ),
        ),
      ),
    );
  }
}
